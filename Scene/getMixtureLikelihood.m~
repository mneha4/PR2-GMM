function [ output_args ] = getMixtureLikelihood(X, )
%X is a sample example(test) with 
%   Detailed explanation goes here
    total = 1;
    for k=1:K
        total = total * pis{k}*NormalDist(X(n,:),mus{k},sigmas{k});  
    end

end

